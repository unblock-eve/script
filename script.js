javascript:(function(){const button=document.createElement('button');button.textContent='Paste and Press Enter';button.style.position='fixed';button.style.top='10px';button.style.left='10px';button.style.zIndex='1000';button.style.padding='10px';button.style.fontSize='14px';document.body.appendChild(button);button.addEventListener('click',async()=>{try{const clipboardText=await navigator.clipboard.readText();const targetDiv=document.querySelector('div[contenteditable="true"]');if(targetDiv){targetDiv.textContent=clipboardText;console.log('Pasted content into the box:',clipboardText);const simulateEnterKey=()=>{const enterEvent=new KeyboardEvent('keydown',{key:'Enter',code:'Enter',keyCode:13,which:13,bubbles:true});targetDiv.dispatchEvent(enterEvent);console.log('Enter key pressed.');};simulateEnterKey();setTimeout(()=>{simulateEnterKey();},200);}else{console.error('Target contenteditable div not found.');}}catch(err){console.error('Failed to access clipboard:',err);}});})();
